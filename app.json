{
  "name": "Python AI Telegram Bot",
  "description": "A smart Telegram bot using Python (Flask), Groq AI, and MongoDB.",
  "keywords": ["python", "flask", "telegram", "bot", "groq", "mongodb"],
  "repository": "https://github.com/YOUR_USERNAME/YOUR_REPO_NAME",
  "env": {
    "TELEGRAM_TOKEN": {
      "description": "Your Telegram Bot Token from @BotFather",
      "required": true
    },
    "GROQ_API_KEY": {
      "description": "Your API Key from Groq Cloud",
      "required": true
    },
    "MONGO_URI": {
      "description": "MongoDB Atlas Connection String",
      "required": true
    }
  },
  "buildpacks": [
    {
      "url": "heroku/python"
    }
  ]
}
